[pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    e2e: end-to-end tests
    unit: unit tests
    slow: slow tests
    concurrency: concurrency tests
addopts =
    -v
    --tb=short
    --strict-markers
